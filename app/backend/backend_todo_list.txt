Back End TO DO List

########################## ENDPOINTS ####################

* GET geo                                             [V]
Tratar retorno para evitar redundâncias ??

* GET weather/current                                 [V]
* GET weather/forecast                                [V]
Traduzir descrições
Verificar se é preciso converter dados de umidade e velocidade do vento, etc
Salvar histórico do usuário

* GET weather/combined                                [ ]
A ideia é combinar as duas consultas em um retorno único,
com todos os dados tratados.


* POST user                                           [V]
Rota de cadastro do usuário.
Rota criada.
Validações implementadas (revisar)
Repository criada, persistindo no banco.

Falta:
Não permitir registrar caso os headers já estejam preenchidos com dados de autenticação.
Tratar dados do cliente no retorno: não trazer senha.
Retornar favoritos do usuário.

* GET user                                            [V]
Rota criada.
Retorna dados do usuário.
Repository criada, recuperando dados do banco.

# Essa rota é apenas para testar a persistência dos dados no banco, não será usada pelo front.

* PUT user                                            [ ]
Atualiza dados do usuário.
Recebe dados pelo corpo da requisição, atualiza somente os que não estiverem vazios.
Validações obrigatórias para cada campo.

## Autenticação: só permitir a requisição se os dados de autenticação estiverem corretos nos headers.

* DELETE user                                         [ ]
Exclui o usuário.

## Autenticação: só permitir a requisição se os dados de autenticação estiverem corretos nos headers.

* POST user/login                                     [V]
Implementada.
  1. Recebe o email e a senha no corpo da requisição
  2. Recupera os dados do usuário pelo email
  3. Compara os dados recebidos com os dados persistidos
  4. Retorna dados do cliente.

Falta:
Tratar os dados do retorno.
Retornar favoritos.

Logout poderá ser feito pelo front, apagando dados do cache que seriam enviados pelo header.

* POST user/favorites                                 [V]
Salva um novo favoritos.
Não permitir se o usuário já possuir 3 salvos.
Autenticar.
* GET user/favorites                                  [V]
Obtém favoritos do usuário.
Autenticar.
* PUT user/favorites                                  [V]
Atualizar favoritos do usuário.
Mesma lógica do PUT user.
Autenticar.
* DELETE user/favorites                               [V]
Exclui um favorito do usuário.
Autenticar.

* GET user/history                                    [ ]
Lista histórico do usuário.
Autenticar.
* DELETE user/history                                 [ ]
Limpa todo o histórico do usuário.
Autenticar.

* GET logs                                            [V]
Rota criada para testar registros.
# Essa rota é apenas para testar a persistência dos dados no banco, não será usada pelo front.

* GET statistics                                      [ ]
Lista estatísticas, baseado nos logs salvos.
Locais mais buscados de todos os tempos.
Do último ano, do último mês, da última semana.
Pode trazer tudo em uma rota ou fazer rotas específicas,
como "statistics/week", "statistics/month", etc

#########################################################
